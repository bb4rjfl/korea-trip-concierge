# Normalize line endings to LF in the repo so linux/amd64 Docker builds (KC)
# and Windows dev checkouts agree. Prevents CRLF leaking into the container.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
